KDB Connector Test Suite
========================

To run tests:
1) Download q from kx.com and extract it in this directory
1) if you are inside UBS clone the submodule and set `-Dkdb.build.ubs=true` jvm arg
1) run `cat xaa.dat xab.dat > clientspot.dat` in the ubs dir 